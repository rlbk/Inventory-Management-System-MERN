generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
  // url      = "mongodb+srv://udemyrlbk:194pZhHaIVu81vox@inventorymanagementsyst.aygvc.mongodb.net/?retryWrites=true&w=majority&appName=InventoryManagementSystem"
}

model Customer {
  id       String   @id @default(auto()) @map("_id") @db.ObjectId
  email    String   @unique
  name     String
  phone    String
  createAt DateTime @default(now())
  updateAt DateTime @updatedAt
}
